dependencies {
	compile project(':opt4j-core')
	compile project(':opt4j-satdecoding')
}

sourceSets {
    main {
        java {
            srcDir 'src'
            exclude '**/Opt4JBenchmarkActivator.java'
            exclude 'test/**'
       }
   }
}
